/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: { unoptimized: true } // simple for demo; replace with proper loader in prod
}
module.exports = nextConfig
